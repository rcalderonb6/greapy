name: docs-build
on:
    pull_request:
        branches:
            - main
            - master
env:
  UV_SYSTEM_PYTHON: 1

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
              with:
                  fetch-depth: 0

            - name: 🚀 Install uv
              uses: astral-sh/setup-uv@v4
            
            - name: 🐍 Set up Python
              uses: actions/setup-python@v5
              with:
                python-version: 3.12

            # - name: Install dependencies
            #   run: |
            #       pip install --no-cache-dir Cython
            #       pip install -r requirements.txt -r requirements_dev.txt
            #       pip install .

            - name: 📦 Install dependencies
              run: |
                uv pip install --no-cache-dir Cython
                uv pip install -r requirements.txt -r requirements_dev.txt
                uv pip install .
                cobaya-install bao.desi_2024_bao_all -p tmp/
                cobaya-install bao.desi_dr2 -p tmp/
            - name: Discover typos with codespell
              run: codespell --skip="*.csv,*.geojson,*.json,*.js,*.html,*cff,*.pdf,*.ipynb,./.git" --ignore-words-list="aci,acount,hist"
            # - name: PKG-TEST
            #   run: |
            #       python -m unittest discover tests/
            - name: Build docs
              run: |
                  mkdocs build

            # - name: Deploy to Netlify
            #   uses: nwtgck/actions-netlify@v2.0
            #   with:
            #       publish-dir: "./site"
            #       production-branch: main

            #       github-token: ${{ secrets.GITHUB_TOKEN }}
            #       deploy-message: "Deploy from GitHub Actions"
            #       enable-pull-request-comment: true
            #       enable-commit-comment: false
            #       overwrites-pull-request-comment: true
            #   env:
            #       NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
            #       NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
            #   timeout-minutes: 10

